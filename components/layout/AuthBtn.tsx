import KakaoLogin from "react-kakao-login";

const AuthBtn = () => {
  // const onSuccess = (result) => {
  //   console.log("success");
  //   console.log("result: ", result);
  // };

  // const onFail = () => {
  //   console.log("faliure");
  // };

  return (
    <div>
      {/* <KakaoLogin
        token="89830a26d1cb1514c090f323165b6804"
        onSuccess={onSuccess}
        onFail={onFail}
        render={({ onClick }) => {
          return (
            <button type="button" onClick={onClick}>
              카카오 로그인
            </button>
          );
        }}
      /> */}
    </div>
  );
};

export default AuthBtn;
